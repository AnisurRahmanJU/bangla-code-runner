```py
// ১. তুলনা অপারেশন
দেখাও(৫ > ৩);          // true
দেখাও(১০ == ১৫);       // false
দেখাও("লিখন" == "লিখন");  // true
দেখাও("বাংলা" != "কোড");  // true

// ২. লজিক্যাল অপারেটর
দেখাও((১০ > ৫) && (২০ < ৩০));  // true  (এবং)
দেখাও((৫ > ১০) || (২০ < ৩০));  // true  (অথবা)
দেখাও(!(৫ > ১০));               // true  (না)

// ৩. স্ট্রিং চেক
দেখাও("লিখন".দৈর্ঘ্য > ০);      // true (স্ট্রিং ফাঁকা না)

// ৪. সংখ্যা চেক
দেখাও(৭ % ২ == ০);              // false (৭ জোড় না বিজোড়)
দেখাও(৮ % ২ == ০);              // true  (৮ জোড়)

// ৫. বুলিয়ান ভ্যারিয়েবল সরাসরি
সঠিক = সত্য;
দেখাও(সঠিক);                   // true

```
