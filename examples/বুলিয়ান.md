```py
// Condition Operation
দেখাও(৫ > ৩);    // সত্য
দেখাও(১০ == ১৫); // মিথ্যা
দেখাও("লিখন" == "লিখন"); // সত্য
দেখাও("বাংলা" != "কোড");  // সত্য

// Logical Operation
দেখাও((১০ > ৫) && (২০ < ৩০)); // সত্য (এবং)
দেখাও((৫ > ১০) || (২০ < ৩০));  // সত্য (অথবা)
দেখাও(!(৫ > ১০)); // সত্য (না)

// String Check
দেখাও("লিখন".দৈর্ঘ্য > ০); // সত্য (স্ট্রিং ফাঁকা না)

// Number Check
দেখাও(৭ % ২ == ০); // মিথ্যা (৭ জোড় না বিজোড়)
দেখাও(৮ % ২ == ০); // সত্য  (৮ জোড়)

// ৫. Directly Boolean Variable
সঠিক = সত্য;
দেখাও(সঠিক); // সত্য

```
