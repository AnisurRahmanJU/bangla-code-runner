```py
// গুণফল (Multiplication)
ধরি ক = ৬;
ধরি খ = ৭;
ধরি ফলাফল_১ = ক * খ;
দেখাও(ফলাফল_১);

// বর্গমূল (Square Root)
ধরি ফলাফল_২ = গণিত.বর্গমূল(২৫);
দেখাও(ফলাফল_২);

// র‌্যান্ডম নম্বর (Random Number)
ধরি ফলাফল_৩ = গণিত.র‌্যান্ডম();
দেখাও(ফলাফল_৩);

// লগ (Logarithm)
ধরি ফলাফল_৪ = গণিত.লগ(১০);
দেখাও(ফলাফল_৪);

// পাই (Pi)
ধরি ফলাফল_৫ = গণিত.পাই;
দেখাও(ফলাফল_৫);
```
