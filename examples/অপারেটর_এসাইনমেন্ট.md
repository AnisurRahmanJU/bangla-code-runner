```py
// Assignment
ক = ৫;
দেখাও(ক);

// Add and Assign
ক += ৩;
দেখাও(ক);

// Subtract and Assign
ক -= ২; 
দেখাও(ক);

// Multiply and Assign
ক *= ৪;
দেখাও(ক);

// Divide and Assign
ক /= ৬;
দেখাও(ক);

// Modulus and Assign
ক %= ৩;
দেখাও(ক);
```
