```py
// যোগ (Addition)
দেখাও(৫ + ৩); // ৮

// বিয়োগ (Subtraction)
দেখাও(৫ - ৩); // ২

// গুণ (Multiplication)
দেখাও(৫ * ৩); // ১৫

// ভাগ (Division)
দেখাও(৬ / ৩); // ২

// বাকি (Modulus)
দেখাও(৭ % ২); // ১

// পূর্ণসংখ্যা ভাগ (Integer Division)
দেখাও(৭ // ২); // ৩ (পূর্ণসংখ্যা ভাগ)
```
