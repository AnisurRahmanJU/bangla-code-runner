```py
ধরি সংখ্যা১ = ১০;
ধরি সংখ্যা২ = ৪;

ধরি যোগ = সংখ্যা১ + সংখ্যা২;
দেখাও(যোগ); 

ধরি বিয়োগ = সংখ্যা১ - সংখ্যা২;
দেখাও(বিয়োগ);

ধরি গুণ = সংখ্যা১ * সংখ্যা২;
দেখাও(গুণ);

ধরি ভাগ = সংখ্যা১ / সংখ্যা২;
দেখাও(ভাগ);

ধরি ভাগশেষ = সংখ্যা১ % সংখ্যা২;
দেখাও(ভাগশেষ);
```
