```py
// Fibonacci Series
ধরি ক = ০, খ = ১, পরবর্তী;

দেখাও(ক);
দেখাও(ক);

লুপ (ধরি গ = ৩; গ <= ১০; গ++) {
  পরবর্তী = ক + খ;
  দেখাও(পরবর্তী);
  ক = খ;
  খ = পরবর্তী;
}

```
```py
//Recursive Fibonacci
ফাংশন ফিবোনাক্কি(ক)
{
  যদি (ক <= ১)
  {
    ফেরত ক;
  }
  নাহলে
  {
    ফেরত ফিবোনাক্কি(ক-১) + ফিবোনাক্কি(ক-২)
  }
}
 
ধরি খ; 
ধরি নাম্বার = ১০;
দেখাও("ফিবোনাক্কি সিরিজ: ");
 
লুপ(খ = ১; খ < নাম্বার; খ++)
  {
    দেখাও(ফিবোনাক্কি(খ));
  }
```
