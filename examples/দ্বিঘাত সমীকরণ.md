```py
ধরি  ক = ১;
ধরি  খ = -৩;
ধরি  গ = ২;

ধরি  ঘ  = খ * খ - ৪ * ক * গ; // ডিসক্রিমিন্যান্ট

যদি (ঘ > ০) {
  ধরি  এক্স১ = (-খ + Math.sqrt(d)) / (২ * ক);
  ধরি  এক্স২ = (-খ - Math.sqrt(d)) / (২ * ক);
  দেখাও("মূলসমূহ: " + এক্স১ + ", " + এক্স২);
} নাহলে যদি (ঘ == ০) {
  ধরি  এক্স = -খ / (২ * ক);
  দেখাও("একক মূল: " + এক্স);
} নাহলে {
  দেখাও("বাস্তব মূল নেই");
}

```
